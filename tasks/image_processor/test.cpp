#include <catch.hpp>

#include "image_processor.cpp"
TEST_CASE("image_processor") {
    REQUIRE(6 == 6);
    // короче у меня ничего симейк не видит и не хочет инклюдить catch.hpp поэтому я хз как это написать
}
